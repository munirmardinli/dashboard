---
x-labels: &default-labels
  com.centurylinklabs.watchtower.enable: true
  container.label.group: management

x-limits: &resource-limits
  mem_limit: "256m"
  mem_reservation: "64m"
  cpu_shares: "512"
  restart: always
  networks:
    frontend:

services:
  dashboard:
    image: docker.io/mardinlimunir/dashboard:latest
    container_name: dashboard-example
    hostname: dashboard-example
    <<: *resource-limits
    ports:
      - target: 80
        published: 4012
        protocol: tcp
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY?:Gemini API key is required}
      GEMINI_MODEL: ${GEMINI_MODEL?:Gemini model is required}
      GITHUB_REPO: ${GITHUB_REPO?:GitHub repository is required}
      GITHUB_TOKEN: ${GITHUB_TOKEN?:GitHub token is required}
      GITHUB_BRANCH: ${GITHUB_BRANCH?:GitHub branch is required}
      GITHUB_OWNER: ${GITHUB_OWNER?:GitHub Owner is required}
      TWILIO_ACCOUNT_SID: ${TWILIO_ACCOUNT_SID:?Twilio Account SID is required}
      TWILIO_AUTH_TOKEN: ${TWILIO_AUTH_TOKEN:?Twilio Auth Token is required}
      TWILIO_WHATSAPP_FROM: ${TWILIO_WHATSAPP_FROM:?Twilio WhatsApp sender number is required}
      TWILIO_WHATSAPP_TO: ${TWILIO_WHATSAPP_TO:?Twilio WhatsApp destination number is required}
    labels:
      <<: *default-labels
      monitoring: dashboard-example

networks:
  frontend:
    external: true
